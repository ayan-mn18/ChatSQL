# ============================================
# ChatSQL Environment Variables
# Copy this file to .env and fill in your values
# ============================================

# Server Configuration
NODE_ENV=development
PORT=3000

# Application Database (PostgreSQL)
# This is the database where ChatSQL stores user data, connections, etc.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=chatsql
DB_USERNM=your_username
DB_PWD=your_password

# JWT Configuration
# Generate a secure secret: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters-long

# SMTP Configuration (Email Service)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
SMTP_FROM_EMAIL=noreply@chatsql.io
SMTP_FROM_NAME=ChatSQL

# Frontend URL (for email links)
FRONTEND_URL=http://localhost:3001

# Redis Configuration (Optional - for caching)
# REDIS_URL=redis://localhost:6379

# CORS Configuration
CORS_ORIGIN=http://localhost:3001

# AI Services (Optional)
# OPENAI_API_KEY=sk-your-openai-api-key
# ANTHROPIC_API_KEY=sk-your-anthropic-api-key

# ============================================
# PRODUCTION NOTES:
# - Always use strong, unique values for JWT_SECRET
# - Use SSL for database connections in production
# - Consider using a secrets manager for sensitive values
# - Use TLS for SMTP in production (port 465 or STARTTLS on 587)
# ============================================
